# Agent 和场景 Prompt 精简指南

## 精简原则

1. **保留所有关键信息**：决策点、流程步骤、重要规则
2. **删除冗余说明**：重复的解释、过多的示例、详细的背景
3. **精简表达**：用更简洁的语言表达相同的意思
4. **通用示例**：使用占位符而不是具体内容

## 精简技巧

### 1. 合并重复信息
```
❌ 冗余：
### 1. 接收意图分析结果
系统会自动分析用户输入，并在消息末尾提供 [意图分析结果]，包含：
- **场景 (scene)**：任务所属的场景类型（如 explore、write_code、debug 等）
- **推荐 Agent (agent)**：最适合处理该任务的 Agent
- **任务复杂度 (complexity)**：simple（简单）或 complex（复杂）

✅ 精简：
系统在消息末尾提供 [意图分析结果]：scene、agent、complexity
```

### 2. 删除过多的示例说明
```
❌ 冗余：
**你应该做的**：
1. 不要分析、不要解释、不要自己回答
2. 直接复制上面的 task 工具调用
3. 立即执行

**错误示例**：
❌ "让我先看看 package.json 的内容..."（不要自己处理）
❌ "我来分析一下这个文件..."（不要自己处理）
❌ "这个任务需要..."（不要分析，直接委托）

**正确示例**：
✅ 直接调用 task 工具（无需任何解释）

✅ 精简：
立即委托，不要自己处理。直接调用 task 工具。
```

### 3. 精简章节标题
```
❌ 冗余：
#### 情况 A：简单任务 + 推荐了专业 Agent
**特征**：complexity = simple，agent ≠ general
**处理方式**：立即委托，不要犹豫

✅ 精简：
简单专业任务 (complexity=simple, agent≠general)：立即委托
```

### 4. 删除详细的步骤说明
```
❌ 冗余：
为每个 Agent 分配合适的 scene：
1. 使用 \`list_scenes\` 查看所有可用的场景
2. 根据 Agent 的职责和任务需求，选择最匹配的 scene
3. 参考 scene 的 description、keywords 和 collaborationHint
4. Scene 决定了 Agent 会加载哪些场景化的 prompt 指导

✅ 精简：
为每个 agent 分配合适的 scene（用 list_scenes 查询）
```

### 5. 合并相似内容
```
❌ 冗余：
### 3. 理解任务（仅复杂任务需要）
- 分析用户的目标和需求
- 识别任务所属的领域
- 判断需要哪些专业能力

### 6. 分析能力需求（复杂任务）
当任务复杂时，思考需要哪些能力：
- 将任务分解为具体步骤
- 识别每个步骤需要的能力
- 不要假设需要哪些能力，而是根据任务本身分析

✅ 精简：
复杂任务：分析需要哪些能力，分解为具体步骤
```

## MainAgent System Prompt 精简计划

### 当前结构（191 行）
1. 核心能力（3 行）
2. 工作流程（150+ 行）
   - 接收意图分析结果
   - 根据意图分析结果决策（3种情况，每种都有详细说明）
   - 理解任务
   - 决策处理方式
   - 发现可用的 Agent 和 Scene
   - 分析能力需求
   - 匹配 Agent
   - 补充缺失能力
   - 规划协作方式
   - 分配 Scene
3. 原则（6 条）
4. 工具和能力层次（4 行）

### 精简后结构（目标：60-80 行）
1. 核心能力（1 行）
2. 工作流程（40-50 行）
   - 意图分析结果（1 行）
   - 简单专业任务（2 行）
   - 简单通用任务（1 行）
   - 复杂任务（5 行）
   - 创建临时 agent（10 行）
   - 子 agent 输出处理（5 行）
   - 协作方式（5 行）
3. 原则（10 行）

### 关键信息（必须保留）
- 意图分析结果的三个字段：scene、agent、complexity
- 三种情况的决策规则
- 0.5 分数线（match_agent）
- 临时 agent 的创建要求（system_prompt + tools）
- description vs system_prompt 的区别
- stream_to_user 的使用场景
- 引用标记格式
- 协作策略类型
- 错误处理原则

### 可以删除的内容
- 详细的步骤说明
- 多个示例
- 错误示例和正确示例的对比
- 重复的解释
- 详细的背景说明
- 能力需求的示例列表

## 下一步

1. 精简 MainAgent System Prompt（src/core/agent/dispatch/MainAgent.ts）
2. 精简 xuanji.yaml
3. 精简其他 agent 配置（software-engineer, stock-analyst 等）
4. 精简场景 prompt（l1-*.yaml, l2-*.yaml）

每个文件精简后，都要确保：
- 所有关键信息都保留
- 决策点清晰
- 流程完整
- 示例通用
