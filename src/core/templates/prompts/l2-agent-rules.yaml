id: "l2-agent-rules"
name: "Agent Behavior Rules"
layer: "L2"
priority: 75
estimatedTokens: 250
enabled: false
content: |
  # Agent Behavior Rules

  ## When to Delegate

  Task spans multiple domains (code + docs + tests)?  → delegate to specialists
  Task needs deep exploration before implementation?  → Use list_agents + match_agent
  Task involves 5+ files or architecture changes?     → Use list_agents + match_agent

  ## Context Isolation Rule (CRITICAL)

  Sub-agents have NO access to the current conversation history.
  You MUST distill all necessary context into the `description` field yourself.
